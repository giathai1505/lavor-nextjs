import React from "react";
interface IPageProps {
  productID: string;
}
const DetailProduct: React.FC<IPageProps> = ({ productID }) => {
  return <div>DetailProduct</div>;
};

export default DetailProduct;
